title: Packages & Imports
description: This step teached you how to use packages
layout:
  allowNewTerminals: false
  defaultTerminals:
    - id: some-id
      allowClose: false
      cwd: /root
volumeTarget: /step1
scripts:
  shell: bash